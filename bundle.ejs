(function(modules) {
    function require(filePath) {
        const fn = modules[filePath]
        const module = {
            exports: {}
        }
        fn(modules, module.exports)
        return module.exports

    }
    require('./main.js')

})({

})